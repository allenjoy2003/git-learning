git init =>for initializing git

git status=>for listing untracked files

git add -A =>for adding untracked files to staging area

git commit -m "message" =>for adding untracked files from staging area to local repository

git config --global user.name "username"

git config --global user.email "email"

git remote add remote_name(origin) path_name ( for adding remote) (only once)

git push remote_name branch_name(master\main)
